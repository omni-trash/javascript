# packages
npm install --save-dev jest
npm install --save-dev @types/jest
npm install --save-dev ts-jest

# if not yet installed
npm install --save-dev typescript

# set up
jest.config.js

# run test (see package.config -> scripts -> test)
npm test

# run test (optional)
install VS Code Extension: Jest Test Explorer
